Material for effector test module of training course.

    app-template-bom.yaml  Template catalog for python web server app
    server-entity-bom.yaml Catalog for entity instantiating the server template
    effector-test.yaml Tests for the effectors, using the entity from server-entity

The first two above are catalog boms, to add to the catalog.

The last above is the YAML for tests on the entities in the app.
